default:
  emulator:
  core:
  options:
    videomode:      default
    ratio:          auto
    video_threaded: false
    smooth:         true
    rewind:         false
    bezel:          none
    forceNoBezel:   true

switch:
  emulator: eden-emu
  core:     eden-emu
  options:
    forceNoBezel: true
    hud_support: false